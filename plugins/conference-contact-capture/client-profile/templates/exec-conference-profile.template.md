# Conference Contact Capture — Profile

> Generated by the `setup` skill on first run. Re-run setup any time to refresh fields.

## Profile completeness

<complete | partial — list missing fields>

## Identity

- Name: <executive name>
- Title: <executive title>
- Company: <company name>
- One-line company description: <one sentence on what the company does>

## Tools

(Provider names only. Tool slugs / MCP endpoints are NOT stored here — they live in the host's MCP config and are discovered at runtime. The `Account` sub-field disambiguates when multiple accounts of the same provider are connected — e.g. work + personal Gmail. Stored as the human-readable alias if set, otherwise the email address, otherwise the underlying account id.)

- Email tool:
  - Provider: <gmail | outlook | other>
  - Account: <alias or email address — captured at setup; orchestrator passes this to the email tool to target the right account>
- Web search/extract:
  - Provider: <tavily | composio_search | other | none>
  - Account: <alias or identifier — usually only one Tavily account; leave blank if single-account>
- CRM:
  - Provider: <notion | hubspot | airtable | salesforce | other | none>
  - Account: <alias or identifier — usually only one CRM account; leave blank if single-account>
  - Target database: <database/page ID — filled during setup if a CRM provider is connected>
  - Field mapping (filled during setup):
    - Name → <field ID/name>
    - Company → <field ID/name>
    - Brief → <field ID/name>
    - Email draft → <field ID/name>

## Email signoff

<detected and confirmed signoff, e.g. "Best, Sam">

## LinkedIn skill

- Onboarded: <yes | no | skipped>
- Last verified: <YYYY-MM-DD>

## Output folder

<absolute path; default ~/Documents/conference-followup/>
